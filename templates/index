<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>بسته‌های اینترنت مودم ZLTX28</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Vazirmatn:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Vazirmatn', sans-serif; background-color: #0b0f19; color: #f3f4f6; }
        .glow-effect { box-shadow: 0 0 25px rgba(14, 165, 233, 0.15); }
        .glow-button:hover { box-shadow: 0 0 20px rgba(14, 165, 233, 0.5); }
    </style>
</head>
<body class="min-h-screen flex flex-col justify-between selection:bg-sky-500 selection:text-white">

    <header class="border-b border-slate-800 bg-slate-900/60 backdrop-blur-md sticky top-0 z-50">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 h-20 flex items-center justify-between">
            <div class="flex items-center space-x-3 space-x-reverse">
                <div class="w-10 h-10 rounded-xl bg-gradient-to-tr from-sky-500 to-indigo-600 flex items-center justify-center glow-effect">
                    <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 3v2m6-2v2M9 19v2m6-2v2M5 9H3m2 6H3m18-6h-2m2 6h-2M7 19h10a2 2 0 002-2V7a2 2 0 00-2-2H7a2 2 0 00-2 2v10a2 2 0 002 2zM9 9h6v6H9V9z"></path></svg>
                </div>
                <div>
                    <h1 class="text-lg font-bold tracking-wide text-white">مودم ZLT X28</h1>
                    <p class="text-xs text-slate-400">پنل اختصاصی اینترنت پرسرعت 5G / 4.5G</p>
                </div>
            </div>
            <div>
                <button onclick="openLoginModal()" class="bg-slate-800 hover:bg-slate-700 text-sky-400 border border-sky-500/30 px-4 py-2 rounded-xl text-sm font-medium transition duration-200 flex items-center gap-2">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 16l-4-4m0 0l4-4m-4 4h14m-5 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h7a3 3 0 013 3v1"></path></svg>
                    ورود مشتریان
                </button>
            </div>
        </div>
    </header>

    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-10 flex-grow w-full">
        <div class="text-center max-w-3xl mx-auto mb-12">
            <span class="bg-sky-500/10 text-sky-400 text-xs font-semibold px-3 py-1 rounded-full border border-sky-500/20 inline-block mb-4">نسل جدید ارتباطات بی‌سیم</span>
            <h2 class="text-3xl sm:text-4xl font-extrabold text-white tracking-tight mb-4">خرید و مدیریت بسته‌های اینترنت مودم</h2>
            <p class="text-slate-400 text-sm sm:text-base leading-relaxed">بسته‌های ویژه و پرسرعت مخصوص مودم‌های سیم‌کارتی ZLT X28. بسته‌ی مورد نظر خود را انتخاب کنید یا با ورود به پنل کاربری، وضعیت مصرف لحظه‌ای خود را بررسی نمایید.</p>
        </div>

        <div class="flex justify-center mb-8">
            <div class="bg-slate-900 p-1.5 rounded-2xl border border-slate-800 inline-flex gap-1">
                <button onclick="switchTab('daily')" id="btn-daily" class="px-6 py-2.5 rounded-xl text-sm font-semibold transition-all duration-200 bg-sky-500 text-white shadow-lg shadow-sky-500/25">روزه</button>
                <button onclick="switchTab('weekly')" id="btn-weekly" class="px-6 py-2.5 rounded-xl text-sm font-semibold transition-all duration-200 text-slate-400 hover:text-white">هفتگی</button>
                <button onclick="switchTab('monthly')" id="btn-monthly" class="px-6 py-2.5 rounded-xl text-sm font-semibold transition-all duration-200 text-slate-400 hover:text-white">ماهانه</button>
            </div>
        </div>

        <div id="packages-container" class="grid grid-cols-1 md:grid-cols-3 gap-6 max-w-6xl mx-auto"></div>
    </main>

    <div id="login-modal" class="fixed inset-0 bg-black/70 backdrop-blur-sm z-50 flex items-center justify-center hidden p-4">
        <div class="bg-slate-900 border border-slate-800 rounded-2xl max-w-md w-full p-6 shadow-2xl relative">
            <button onclick="closeLoginModal()" class="absolute top-4 left-4 text-slate-400 hover:text-white">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path></svg>
            </button>
            
            <div id="login-form-view">
                <div class="text-center mb-6">
                    <h3 class="text-xl font-bold text-white mb-2">ورود به پنل کاربری</h3>
                    <p class="text-xs text-slate-400">نام کاربری و رمز عبور خود را جهت مشاهده مصرف لحظه‌ای وارد کنید.</p>
                </div>
                <form onsubmit="handleLogin(event)" class="space-y-4">
                    <div>
                        <label class="block text-xs font-medium text-slate-300 mb-1">نام کاربری</label>
                        <input type="text" id="username" required class="w-full bg-slate-950 border border-slate-800 rounded-xl px-4 py-3 text-sm text-white focus:outline-none focus:border-sky-500 transition" placeholder="مثال: user123">
                    </div>
                    <div>
                        <label class="block text-xs font-medium text-slate-300 mb-1">رمز عبور</label>
                        <input type="password" id="password" required class="w-full bg-slate-950 border border-slate-800 rounded-xl px-4 py-3 text-sm text-white focus:outline-none focus:border-sky-500 transition" placeholder="••••••••">
                    </div>
                    <button type="submit" class="w-full bg-sky-500 hover:bg-sky-400 text-white font-semibold py-3 rounded-xl transition duration-200 shadow-lg shadow-sky-500/20">ورود به سیستم</button>
                </form>
            </div>

            <div id="dashboard-view" class="hidden">
                <div class="text-center mb-6">
                    <span class="bg-emerald-500/10 text-emerald-400 text-xs px-3 py-1 rounded-full border border-emerald-500/20">متصل به مودم زیتل</span>
                    <h3 class="text-lg font-bold text-white mt-3" id="welcome-user">خوش آمدید</h3>
                </div>
                
                <div class="space-y-4 bg-slate-950 p-4 rounded-xl border border-slate-800 mb-6">
                    <div class="flex justify-between items-center text-sm">
                        <span class="text-slate-400">حجم کل بسته:</span>
                        <span class="font-bold text-white" id="total-traffic"></span>
                    </div>
                    <div class="flex justify-between items-center text-sm">
                        <span class="text-slate-400">حجم مصرف شده:</span>
                        <span class="font-bold text-amber-400" id="used-traffic"></span>
                    </div>
                    <div class="flex justify-between items-center text-sm">
                        <span class="text-slate-400">حجم باقیمانده:</span>
                        <span class="font-bold text-emerald-400" id="remain-traffic"></span>
                    </div>
                    <div class="flex justify-between items-center text-xs text-slate-500 pt-1">
                        <span class="text-sky-400 cursor-pointer hover:underline" onclick="refreshStatus()">بروزرسانی لحظه‌ای 🔄</span>
                    </div>
                </div>

                <button onclick="logout()" class="w-full bg-rose-500/10 hover:bg-rose-500/20 text-rose-400 border border-rose-500/30 font-medium py-2.5 rounded-xl transition duration-200 text-sm">خروج از حساب</button>
            </div>
        </div>
    </div>

    <footer class="border-t border-slate-800 bg-slate-900/40 py-6 text-center text-xs text-slate-500">
        <p>تمامی حقوق برای مدیریت مودم ZLT X28 محفوظ است. © 2026</p>
    </footer>

    <script>
        const packagesData = {
            daily: [{ name: "بسته ۱ روزه", data: "۲ گیگابایت", price: "۲۰,۰۰۰", desc: "سرعت فوق‌العاده مناسب وب‌گردی", tag: "پرفروش" }],
            weekly: [{ name: "بسته ۷ روزه", data: "۷ گیگابایت", price: "۷۰,۰۰۰", desc: "مناسب برای استریم متوسط", tag: "پرفروش" }],
            monthly: [{ name: "بسته ۳۰ روزه", data: "۵۰ گیگابایت", price: "۶۰۰,۰۰۰", desc: "حجم بالا برای دانلود مداوم", tag: "پرفروش" }]
        };

        function switchTab(category) {
            ['daily', 'weekly', 'monthly'].forEach(cat => {
                const btn = document.getElementById(`btn-${cat}`);
                btn.className = cat === category 
                    ? "px-6 py-2.5 rounded-xl text-sm font-semibold transition-all duration-200 bg-sky-500 text-white shadow-lg shadow-sky-500/25"
                    : "px-6 py-2.5 rounded-xl text-sm font-semibold transition-all duration-200 text-slate-400 hover:text-white";
            });

            const container = document.getElementById('packages-container');
            container.innerHTML = '';

            packagesData[category].forEach(pkg => {
                container.innerHTML += `
                    <div class="bg-slate-900 border border-slate-800 rounded-2xl p-6 flex flex-col justify-between relative hover:border-sky-500/50 transition duration-300 group">
                        <div>
                            <div class="flex justify-between items-start mb-4">
                                <span class="bg-sky-500/10 text-sky-400 text-xs font-semibold px-3 py-1 rounded-full border border-sky-500/20">${pkg.tag}</span>
                                <span class="text-xs text-slate-400 font-mono">ZLT-X28</span>
                            </div>
                            <h3 class="text-lg font-bold text-white mb-2 group-hover:text-sky-400 transition">${pkg.name}</h3>
                            <p class="text-slate-400 text-xs leading-relaxed mb-6">${pkg.desc}</p>
                            <div class="bg-slate-950 p-4 rounded-xl border border-slate-800/80 mb-6 flex items-center justify-between">
                                <div><span class="text-xs text-slate-500 block">حجم بسته</span><span class="text-base font-bold text-emerald-400">${pkg.data}</span></div>
                                <div class="text-left"><span class="text-xs text-slate-500 block">مبلغ</span><span class="text-lg font-extrabold text-white">${pkg.price}</span></div>
                            </div>
                        </div>
                        <button class="w-full bg-slate-800 hover:bg-sky-500 text-slate-200 hover:text-white font-medium py-3 rounded-xl transition duration-200 text-sm glow-button border border-slate-700 hover:border-sky-400">انتخاب و خرید</button>
                    </div>
                `;
            });
        }

        function openLoginModal() { document.getElementById('login-modal').classList.remove('hidden'); }
        function closeLoginModal() { document.getElementById('login-modal').classList.add('hidden'); }

        async function handleLogin(e) {
            e.preventDefault();
            const username = document.getElementById('username').value;
            const password = document.getElementById('password').value;

            const response = await fetch('/api/login', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ username, password })
            });
            const data = await response.json();

            if(data.success) {
                document.getElementById('welcome-user').innerText = `خوش آمدید، کاربر عزیز (${data.username})`;
                document.getElementById('total-traffic').innerText = data.total + ' گیگابایت';
                document.getElementById('used-traffic').innerText = data.used + ' گیگابایت';
                document.getElementById('remain-traffic').innerText = data.remaining + ' گیگابایت';
                
                document.getElementById('login-form-view').classList.add('hidden');
                document.getElementById('dashboard-view').classList.remove('hidden');
            } else {
                alert(data.message);
            }
        }

        function logout() {
            document.getElementById('dashboard-view').classList.add('hidden');
            document.getElementById('login-form-view').classList.remove('hidden');
            document.getElementById('username').value = '';
            document.getElementById('password').value = '';
        }

        function refreshStatus() { alert('اطلاعات مصرف لحظه‌ای به‌روزرسانی شد.'); }

        window.onload = () => switchTab('daily');
    </script>
</body>
</html>
